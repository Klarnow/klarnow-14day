export const metadata = {
  title: "Get More Leads | Klarnow",
  description: "The managed growth system for UK service businesses. Answer more calls, book more customers, and grow without the gaps.",
};

export default function MoreLeadsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
